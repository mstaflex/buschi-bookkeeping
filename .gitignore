# Secrets – never commit real credentials!
# secrets/*.txt are tracked as PLACEHOLDER files so Docker bind-mounts resolve.
# Bootstrap overwrites them with real values on first run.
.env
secrets/*.env

# Docker-Volumes (falls lokal gemountet)
data/

# Backup-Dateien
backups/
*.sql.gz

# Editor
.vscode/
*.swp
*~
