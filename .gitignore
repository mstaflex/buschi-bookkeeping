# Secrets – never commit real credentials!
# Secrets are stored in a Docker named volume (secrets_data), not on the host.
.env
secrets/

# Docker-Volumes (falls lokal gemountet)
data/

# Backup-Dateien
backups/
*.sql.gz

# Editor
.vscode/
*.swp
*~
